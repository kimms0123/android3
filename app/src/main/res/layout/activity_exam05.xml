<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:columnCount="4"
    android:rowCount="2">
    <!--
        android:columnCount="4": 그리드 레이아웃의 행 수 지정
        android:rowCount="2" : 그리드 레이아웃의 열 수 지정
     -->
    <Button
        android:layout_column="0"
        android:layout_row="0"
        android:layout_rowSpan="2"
        android:layout_gravity="fill_vertical"
        android:text="1"/>
    <!--
        layout_column, layout_row: 현재 버튼이 0행 0열에 위치하도록 지정
        layout_rowSpan: 행을 확장
        layout_gravity="fill_vertical": 버튼이 확장된 셀의 수직 방향에 꽉 채워짐
     -->
    <Button
        android:layout_column="1"
        android:layout_row="0"
        android:layout_columnSpan="2"
        android:layout_gravity="fill_horizontal"
        android:text="2"/>
    <!--
        layout_columnSpan: 열 확장
        layout_gravity="fill_horizontal": 버튼의 확장된 셀의 수직 방향에 꽉 채워짐
     -->
    <Button
        android:layout_column="3"
        android:layout_row="0"
        android:text="3"/>

    <Button
        android:layout_column="1"
        android:layout_row="1"
        android:text="4"/>
    <Button
        android:layout_column="2"
        android:layout_row="1"
        android:text="5"/>
    <Button
        android:layout_column="3"
        android:layout_row="1"
        android:text="6"/>

</GridLayout>